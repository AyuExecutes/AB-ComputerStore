/**
 * Interface for the SearchPanel to tell the parent that a selection has changed
 */
public interface SearchPanelCallback {
    /**
     * Method to call when a selection has changed
     */
    void selectionChange();
}
