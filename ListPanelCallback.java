/**
 * Interface for ListPanel to communicate with the parent
 */
public interface ListPanelCallback {
    /**
     * method to call when the selection has changed
     */
    void selectionChange();
}
